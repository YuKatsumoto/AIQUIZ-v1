# AIQUIZ 開発進捗・タスク管理
最終更新: 2026-04-05 (v2)

---

## 📁 プロジェクト構成

```
AIQUIZ-v1/AIQUIZ-v1/
├── 3D_enginefree.py          # エントリーポイント (3Dモード)
├── 2D_pygame.py              # 2Dモード (レガシー)
├── offline_bank.json          # オフライン問題バンク (~700KB)
├── add_offline_4c_quizzes.py  # 4択問題一括追加スクリプト
├── game/
│   ├── app/
│   │   └── main_3d.py         # メインループ・入力処理
│   ├── core/
│   │   ├── constants.py       # 定数定義
│   │   ├── game_state.py      # ゲーム状態管理・ロジック全般
│   │   ├── quiz_provider.py   # クイズ供給 (オフライン/オンライン共通)
│   │   ├── providers/
│   │   │   └── online_fetch.py  # LLM経由のオンライン問題取得
│   │   ├── ratings/           # 問題レーティングシステム
│   │   └── validation/        # 問題バリデーション
│   ├── audio/
│   │   └── synth.py           # プロシージャル効果音生成
│   ├── render/
│   │   ├── renderer.py        # 3D描画エンジン (moderngl)
│   │   └── math3d.py          # 行列・ベクトル計算ユーティリティ
│   └── ui/
│       └── hud.py             # HUD描画 (スコア・問題文・メニュー)
├── server/                    # サーバー関連
├── tools/
│   └── qa_audit.py            # 問題バンク品質管理スクリプト
└── tests/                     # テストコード
```

---

## ✅ 完了済みタスク

### カメラ・視点
- [x] 一人プレイ時: 一人称視点 (FPV) で表示
- [x] 二人プレイ時: カメラ固定の三人称視点 (TPV) で表示
- [x] 二人プレイ時にカメラがキャラに追従しないよう固定

### ゲームプレイ
- [x] 壁の迫る速度を問題難度・文章長に基づいて動的に調整
  - 長い文章の問題 → 壁が遅くなる (wall_speed_min: 3.0)
  - 連続正解すると壁が速くなる (wall_speed_max: 9.0)
- [x] 二人プレイ時にゲームオーバーになったプレイヤーの残像が残るバグを修正
- [x] 一人プレイ時に不要な Player 2 要素が表示されていたのを除去

### 難易度「難しい」(4択モード)
- [x] ゲームロジック: `num_choices` プロパティで難しい=4択を判定
- [x] オンライン問題: 4択形式の問題を受け入れるよう `online_fetch.py` を修正
- [x] オフライン問題: 4択形式の問題を受け入れるよう `quiz_provider.py` を修正
- [x] ドア配置: 4択時のドア位置を `(-4.2, -1.4, 1.4, 4.2)` に設定
- [x] ドア幅: 4択時は `door4_half_width = 0.65` で隙間を確保
- [x] ドア描画: scale X を `0.85` に変更してドア間に壁が見えるように修正
- [x] ラベル更新: キャッシュキーを `id(quiz_object)` に変更し、2問目以降もラベルが正しく更新されるように修正

### オフライン問題バンク
- [x] OFFLINEモードで問題生成が途中で止まるバグを修正
- [x] 「難しい」用の4択問題を大量追加 (算数・国語・理科 各学年)
  - 算数: 各学年80問 × 6学年 = 480問 (ランダム生成)
  - 国語: 各学年5問 × 10バリエーション × 6学年 = 300問
  - 理科: 各学年5問 × 10バリエーション × 4学年 (3~6年) = 200問
  - **合計: 約980問以上の4択問題**
- [x] 社会科の問題を全削除 (195問を削除)

### 演出・エフェクト
- [x] 音声合成: `pygame.sndarray` を用いたプロシージャルな効果音生成 (`synth.py`)
- [x] パーティクル: 正解時のパーティクル飛散エフェクト強化
- [x] 爆発・カメラ: 不正解時の爆発アニメーション・動的カメラ演出 (Death Cam) 追加

---

## 🔧 既知の課題・バグ

### 高優先度
- [x] ~~**バッファ枯渇リスク**: `advance_after_correct` で LLM 生成が間に合わずバッファが空になると `IndexError` が発生する可能性~~ → **修正済み**: バッファ空時に PRELOADING 状態に遷移するガード処理を追加
- [x] ~~**4択問題の生成遅延**: オンラインモードで4択問題はLLM生成に時間がかかり、ローディング画面が長くなる場合がある~~ → **改善済み**: バッファ先読み数の増加 (4→6)、4択時の解説文字数制限を短縮 (15→10字)

### 中優先度
- [x] ~~**社会科が科目リストに未登録**~~ → 社会科は削除済み (問題バンクからも全削除)

4. **問題の重複**: オフライン問題バンクに同一内容の問題が複数含まれるケースがある (特に算数1年)

### 低優先度
5. **テスト不足**: `tests/` ディレクトリの網羅率向上が必要
6. **2D モード (`2D_pygame.py`) との互換性**: 4択モードが2Dに対応しているか未確認

---

## 📋 次のタスク候補

### 機能追加
- [ ] 社会科を科目リスト (`SUBJECTS`) に追加して選択可能にする
- [ ] ローディング画面のUX改善 (プログレスバー、ヒント表示など)
- [ ] ゲームクリア時の結果画面UI (成績サマリー・正答率グラフなど)

### 安定性向上
- [x] **バッファ枯渇時の IndexError 回避ガード追加**
- [x] **オンラインの4択問題生成速度の改善** (出力を短縮し、バッファ先読みを強化)
- [x] **Gemini 2.5 Flash を用いた解説の2段階非同期生成** (出題時に裏で解説テキストを生成し、ゲームオーバー画面で動的に更新する)
- [ ] ユニットテストの追加

### コンテンツ
- [ ] 英語の問題を追加 (英語科目)
- [ ] 画像付き問題への対応
- [x] 問題の品質管理 (重複排除・誤答チェック) → `tools/qa_audit.py` で実施済み (2168→793問)

### デプロイ
- [ ] ビルド・パッケージング (exe化など)
- [ ] サーバーサイドとの連携テスト

---

## ⚙️ 技術メモ

### 動作環境
- Python 3.10 (`C:/Users/kykat/AppData/Local/Programs/Python/Python310/python.exe`)
- 依存: pygame, moderngl, numpy, (+ LLMモード時は dotenv, httpx)

### 起動方法
```bash
cd c:\AIQUIZ\AIQUIZ-v1\AIQUIZ-v1
python 3D_enginefree.py
```

### 主要パラメータ (GameTuning)
| パラメータ | 値 | 説明 |
|---|---|---|
| player_speed | 7.6 | プレイヤー移動速度 |
| wall_speed | 6.8 | 壁の基本速度 |
| wall_speed_min | 3.0 | 壁の最低速度 (長文問題時) |
| wall_speed_max | 9.0 | 壁の最高速度 (連続正解時) |
| door4_xs | (-4.2, -1.4, 1.4, 4.2) | 4択ドア配置 |
| door4_half_width | 0.65 | 4択ドアの半幅 |

### ラベルキャッシュ
- キャッシュキー: `id(quiz_object)` を使用
- 理由: テキストベースのキーだと異なる問題で衝突し、ラベルが更新されないバグがあった
